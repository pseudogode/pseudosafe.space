server {
  listen 80;
  server_name _;

  # bridge-client is built with base=/bridge/, so its files live under /bridge.
  location /bridge/ {
    alias /usr/share/nginx/html/bridge/;
    try_files $uri $uri/ /bridge/index.html;
  }

  location = / {
    return 302 /bridge/;
  }
}
